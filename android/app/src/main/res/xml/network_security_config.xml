<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (ws://, http://) on the local network for dev/demo.
         Production should use wss:// and remove this base-config. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
